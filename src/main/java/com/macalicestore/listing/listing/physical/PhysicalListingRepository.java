package com.macalicestore.listing.listing.physical;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PhysicalListingRepository extends JpaRepository<PhysicalListing, Long> {
}
